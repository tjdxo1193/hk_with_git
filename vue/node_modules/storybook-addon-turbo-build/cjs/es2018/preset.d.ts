import type * as webpack from "webpack";
import type { PresetOptions } from "./types";
declare const _default: {
    managerWebpack(config: webpack.Configuration, options: Partial<PresetOptions>): Promise<webpack.Configuration>;
    webpack(config: webpack.Configuration, options: Partial<PresetOptions>): Promise<webpack.Configuration>;
};
export default _default;
